import React from "react";
import moment from 'moment'

const Calendar = () => {
    const value = moment()
    const startDay = value
    return (
        <div>Calendar</div>

    )
}

export default Calendar;